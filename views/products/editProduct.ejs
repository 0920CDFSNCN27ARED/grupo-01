<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../partials/head.ejs") %>

        <title>EDITAR PRODUCTO | Winderful</title>
    </head>
    <body>
        <%- include("../partials/header.ejs") %>
        <main class="main-newProduct">
            <section class="reveal">
                <p>aca se va cargando lo que pone la persona</p>
            </section>
            <section class="form form-newProduct">
                <form
                    action="/productos/<%= product.id %>?_method=PUT"
                    class="form"
                    method="POST"
                    enctype="multipart/form-data"
                >
                    <label for="productName">Nombre del vino</label>
                    <input
                        value="<%= product.productName %>"
                        type="text"
                        name="productName"
                        id="productName"
                    />
                    <label for="productDescription">Descripción</label>
                    <textarea
                        name="productDescription"
                        id="productDescription"
                        cols="30"
                        rows="10"
                        placeholder="Agregue detalles que se aprecien a la vista, olores y sabores en boca."
                    >
                        <%= product.description %>
                    </textarea>
                    <label for="productGrape">Varietal </label>
                    <input
                        type="text"
                        name="productGrape"
                        id="productGrape"
                        value="<%= product.grape %>"
                    />
                    <label for="productYear">Añada</label>
                    <input
                        type="number"
                        name="productYear"
                        id="productYear"
                        value="<%= product.year %>"
                    />
                    <label for="productTemperature"
                        >Temperatura de servicio</label
                    >
                    <input
                        type="number"
                        name="productTemperature"
                        id="productTemperature"
                        value="<%= product.temperature %>"
                    />
                    <label for="productAged">Crianza</label>
                    <input
                        type="number"
                        name="productAged"
                        id="productAged"
                        value="<%= product.aged %>"
                    />

                    <label for="productPicture">Foto del producto</label>
                    <input
                        type="file"
                        name="productPicture"
                        id="productPicture"
                    />
                    <label for="productPrice">Precio</label>
                    <input
                        type="number"
                        name="productPrice"
                        id="productPrice"
                        value="<%= product.price %>"
                    />

                    <label for="productQuantity">Stock</label>
                    <input
                        type="number"
                        name="productQuantity"
                        id="productQuantity"
                        value="<%= product.stock %>"
                    />

                    <label for="productPairing">Marida bien con:</label>
                    <input
                        type="text"
                        name="productPairing"
                        id="productPairing"
                        value="<%= product.pairing %>"
                    />

                    <button type="submit" class="button create">
                        Editar producto
                    </button>
                </form>
                <form
                    class="form"
                    method="POST"
                    action="/productos/<%= product.id%>/edit?_method=DELETE"
                >
                    <button type="submit" class="button erase">
                        Borrar Producto
                    </button>
                </form>
            </section>
        </main>
        <%- include("../partials/footer.ejs") %>
    </body>
</html>
